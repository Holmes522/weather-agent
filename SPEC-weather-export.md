# Weather Export Specification

## Goal

用户可用自然语言把最近一次真实天气查询结果或多城市行程天气导出为 Word、Excel、PDF 或 Markdown，并通过一次性风格的临时链接下载。

## Inputs

- 导出动作词：`导出`、`保存`、`生成`、`输出`、`下载`、`存储`、`做成`、`整理成`。
- 格式别名：Word/Docx、Excel/Xlsx/Execl、PDF、Markdown/MD。
- 数据来源：同一 `session_id` 最近一次由天气客户端返回的结构化天气结果，或本次多城市行程查询的结构化结果。
- 同一句可同时包含天气查询和导出要求，例如“把深圳和广州明天天气导出为 Excel”。
- 行程导出可包含出发地、目的地顺序、每城停留天数或总天数；行程从明天开始。

## Outputs

- `POST /chat` 在成功导出时增加 `export`：`id`、`format`、`filename`、`download_url`。
- `GET /api/exports/<export_id>` 返回附件，类型分别为 DOCX、XLSX、PDF 和 UTF-8 Markdown。
- 文件至少包含城市、日期、温度、天气状况、湿度、风速、是否有雨、建议和天气服务来源。
- 行程文件中的每条天气记录都必须有针对该日天气生成的出行建议，并包含汇总出行清单。
- 成功生成文件后，聊天响应只返回简短完成提示和下载信息，不重复输出天气表格或天气卡片。

## Behavior

- 只有导出动作但没有格式时，提示用户选择四种格式，不生成文件。
- 没有可用天气快照时，提示先查询天气，不使用模型编造数据。
- 导出请求不改变最近天气快照；导出链接过期后返回 404。
- 文件名由服务端生成并清洗，用户不能指定服务器路径。
- 当用户明确说“依次”“按顺序”或“先…再…”时，按目的地顺序分配日期。例如每个城市待一天时，第一个城市查明天，第二个查后天，依次类推。
- 当用户未说明顺序但给出总行程天数 N 时，每个目的地都导出未来第 1 至 N 天的天气，便于用户自行调整顺序。
- “住在/居住在/从…出发”的城市只作为出发地元数据，不自动加入目的地天气记录。
- 若同时给出每城停留天数和目的地数量，总天数为二者乘积；若另有显式总天数，则以显式总天数为准并校验可执行性。

## Safety and limits

- 文件只保存在有 TTL、数量上限和大小上限的进程内存储中。
- 导出 ID 使用不可预测随机值；响应设置 `Cache-Control: no-store`。
- 单次最多 5 个目的地、7 个行程日和 35 条天气记录，不执行宏、公式、HTML 或用户提供的模板。

## Acceptance tests

- 四种格式都能生成并由对应解析库重新打开。
- `/chat` 支持先查天气后导出，也支持同句查询并导出。
- “依次查询北京、深圳、广州、长沙，每个城市待一天”分别请求未来第 1、2、3、4 天。
- 未指定顺序的 3 天双城行程生成 2 × 3 = 6 条记录。
- Excel 可重新打开，逐行建议非空且包含“出行清单”工作表；成功响应不含 `weather`、`results` 或 `cities`。
- 缺少格式、缺少快照、未知/过期 ID 均返回稳定且可理解的结果。
