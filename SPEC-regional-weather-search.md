# 区域天气现象搜索规格

## 目标

让用户可以询问“现在哪里在打雷下雨”，并能在下一轮用“湖南省有哪些地方”缩小搜索范围。该能力查询实时数据，不把范围问题误当作城市名称。

## 用户可见行为

1. “哪里/哪些地方/哪些城市”与“下雨、降雨、雷雨、打雷、雷暴、雷电”同时出现时，识别为区域天气现象搜索。
2. 未指定范围时默认搜索中国；回答必须说明结果来自“当前监测的主要城市”，不宣称覆盖全国每个地点或替代天气雷达。
3. 指定省份时只搜索该省纳入监测的城市。
4. 后续消息只写省份和“有哪些地方”时，继承上一轮的天气现象。例如上一轮搜索雷雨，下一轮“湖南省有哪些地方”继续搜索湖南雷雨，而不是回答行政区列表。
5. 没有匹配地点时返回正常回答并保留区域搜索上下文，不能产生 `CITY_NOT_FOUND`。
6. 普通单城市天气、多城市天气和普通聊天保持现有响应契约。

## 接口契约

`POST /chat` 请求结构不变。区域搜索成功响应新增以下可选字段：

```json
{
  "intent": "regional_weather",
  "display_mode": "text",
  "scope": "湖南",
  "phenomena": ["thunderstorm"],
  "cities": ["长沙"],
  "results": []
}
```

- `scope`：本轮搜索范围。
- `phenomena`：标准化现象；当前支持 `rain` 和 `thunderstorm`。
- `cities`：匹配地点名称，允许为空。
- `results`：匹配地点的结构化当前天气，允许为空。
- 原有 `answer`、`provider`、`session_id` 字段继续返回。

## 模块边界

- `regional_weather.py`：识别区域查询、维护有界监测目录、筛选结果并生成回答。
- `weather_client.OpenMeteoClient.get_current_many`：使用官方多坐标请求能力，一次批量取得当前天气；严格验证响应数量和字段。
- `session_store.ConversationContext`：以可选字段保存最近一次区域范围和天气现象。
- `app.py`：在模型及单城市解析之前处理区域搜索，保证实时天气必须经过工具数据。

## 安全与资源上限

- 区域范围只能映射到内置监测目录，用户不能控制请求 URL。
- 单次扫描最多 64 个监测点，使用既有 HTTPS 固定主机、连接/读取超时。
- 外部 JSON 按现有 `WeatherDataError` 规则校验；数量不一致视为上游数据错误。
- 不记录 API Key，不将第三方响应当作指令。

## 验收场景

- “哪里在打雷和下雨”不会产生城市名称错误，且执行中国范围扫描。
- “告诉我现在哪里在打雷下雨”之后发送“湖南省有哪些地方”，第二轮搜索湖南并继承雷雨现象。
- “湖南省哪里在下雨”直接搜索湖南降雨。
- “深圳明天天气怎么样”仍按原有单城市流程回答。
